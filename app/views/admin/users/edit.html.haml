- content_for :title do
    Edit #{@user.login}
= render partial: 'form', object: @user, as: :user
- content_for :toolbar do
    %dl
        %dt Actions
        %dd
            %ul.link-list
                %li= link_to("view this user", admin_user_path(@user), class: "glyph-show")
