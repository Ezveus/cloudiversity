%tr
    %td= discipline.name
    %td
        = link_to('View', admin_discipline_path(discipline), class: 'button glyph-button glyph-show')
        = link_to('Edit', edit_admin_discipline_path(discipline), class: 'button glyph-button glyph-edit')
        = link_to('Delete', admin_discipline_path(discipline), method: :delete, class: 'button glyph-button glyph-delete')
